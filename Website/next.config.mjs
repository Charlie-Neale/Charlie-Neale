/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Charlie-Neale',
  assetPrefix: '/Charlie-Neale/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
